type DashboardProps = {};

export const Dashboard: React.FC<DashboardProps> = (props) => {
    console.log({ props });
    return (
        <div>
            <h2>Dashboard works!</h2>
        </div>
    )
}